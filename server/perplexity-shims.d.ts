// Optional fallback module — installed only when
// PERPLEXITY_USE_CYCLETLS=1 is set. Declared here so the perplexity-client
// keeps typechecking without `cycletls` in node_modules.
declare module "cycletls";
